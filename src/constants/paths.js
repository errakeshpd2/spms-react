const base = '/';

export default {
  BASE: base,
  LOGIN: `${base}login`,
  LOGOUT: `${base}logout`,
  PROFILE: `${base}profile`,
  TICKETS: `${base}tickets`,
  ACTIVITY_LOGS: `${base}activity_logs`
};
